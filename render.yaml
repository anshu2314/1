services:
  - type: web
    name: poketwo-catcher
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run db:push --force && npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: poketwo-db
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: PORT
        value: 10000
