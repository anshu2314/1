## Packages
framer-motion | Complex animations for the dashboard cards and transitions
recharts | Analytics charts for catch rates and coin stats
lucide-react | Icons for the UI (already in base, but emphasizing usage)
date-fns | Formatting timestamps for logs
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
- Tailwind Config needs to extend colors for "cyber-purple", "neon-green", "neon-red", "holographic-blue".
- Font families: 'Orbitron' for headers, 'Rajdhani' for data/numbers, 'Inter' for body.
- Polling is required for real-time status updates (accounts and logs).
